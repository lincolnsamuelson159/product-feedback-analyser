# Required
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Jira credentials (get token from https://id.atlassian.com/manage-profile/security/api-tokens)
JIRA_EMAIL=your-email@example.com
JIRA_API_TOKEN=your-api-token

# Jira configuration (defaults shown - change if using a different Jira instance)
JIRA_URL=https://boardiq.atlassian.net
JIRA_BOARD_ID=BPD

# Salesforce credentials (optional - for sales data integration)
# To get your SFDX Auth URL:
#   1. Run: sf org login web (authenticate in browser)
#   2. Run: sf org display --verbose --json | grep sfdxAuthUrl
#   3. Copy the full URL starting with "force://"
# SALESFORCE_AUTH_URL=force://PlatformCLI::xxxxx@yourorg.my.salesforce.com

# Gong credentials (optional - for call transcript analysis)
# Get these from your Gong admin: Settings > Ecosystem > API
# GONG_ACCESS_KEY=your-access-key
# GONG_ACCESS_SECRET=your-access-secret

# Optional (for email reports)
# SENDGRID_API_KEY=SG.xxxxx
# EMAIL_FROM=notifications@yourdomain.com
# EMAIL_TO=your-email@example.com
